# youtube to mp3

paste one or more youtube links and download them as mp3s. supports batch conversion.

**requires:** python, ffmpeg on PATH

```
pip install -r requirements.txt
python app.py
```

open `http://localhost:5000`, paste links (one per line), hit convert.
