# youtubetomp3+4

paste one or more youtube links and download them as mp3 or mp4. supports batch conversion.

**requires:** python, ffmpeg on PATH

```
pip install -r requirements.txt
python app.py
```

open `http://localhost:5000`, paste links (one per line), choose mp3 or mp4, then hit convert.
